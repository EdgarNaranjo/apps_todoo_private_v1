<odoo>
    <data>
        <record id="view_move_form_inherit_sale" model="ir.ui.view">
            <field name="name">account.move.inherit.sale</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- Auto-complete could be done from either a invoice either a sale order -->
                 <xpath expr="//group[@id='header_right_group']" position="after">
                    <group name="sale_auto">
                        <field name="sale_id" invisible="1"/>
                        <label for="sale_invoice_id" string="Auto-Complete" class="oe_edit_only"
                               invisible="state != 'draft' or move_type != 'out_invoice'"/>
                        <div name="journal_div" class="d-flex">
                            <field name="sale_invoice_id" nolabel="1"
                                invisible="state != 'draft' or move_type != 'out_invoice'"
                                class="oe_edit_only"
                                domain="partner_id and [('company_id', '=', company_id), ('partner_id.commercial_partner_id', '=', commercial_partner_id), ('sale_order_id', '!=', False)] or [('company_id', '=', company_id), ('sale_order_id', '!=', False)]"
                                placeholder="Select a sale order or an invoice"
                                context="{'show_total_amount': True}"
                                options="{'no_create': True, 'no_open': True}" readonly="state != 'draft'"/>
                        </div>
                    </group>
                    <field name="invoice_sale_order_id" invisible="1"/>
                </xpath>
                <!-- Add link to sale_line_id to account.move.line -->
                <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='price_total']" position="after">
                    <field name="sale_line_id" invisible="1"/>
                    <field name="sale_id" column_invisible="parent.move_type != 'out_invoice'" optional="hide"/>
                </xpath>
                <xpath expr="//field[@name='line_ids']/list/field[@name='company_id']" position="after">
                    <field name="sale_line_id" invisible="1"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
